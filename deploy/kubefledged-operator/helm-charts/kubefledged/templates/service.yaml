apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubefledged.fullname" . }}
  labels:
    {{- include "kubefledged.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubefledged.selectorLabels" . | nindent 4 }}
